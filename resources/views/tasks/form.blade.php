<form method="POST" action="{{ $formAction }}">@csrf @if ($method !== 'POST') @method($method) @endif
<div class="field"><label for="task_name">Task name</label><input id="task_name" name="task_name" value="{{ old('task_name', $task?->task_name) }}" required autofocus></div>
<div class="field"><label for="description">Description</label><textarea id="description" name="description">{{ old('description', $task?->description) }}</textarea></div>
<div style="display:grid; grid-template-columns:1fr 1fr; gap:14px;"><div class="field"><label for="status">Status</label><select id="status" name="status"><option value="Pending" @selected(old('status', $task?->status ?? 'Pending') === 'Pending')>Pending</option><option value="Completed" @selected(old('status', $task?->status) === 'Completed')>Completed</option></select></div><div class="field"><label for="due_date">Due date</label><input id="due_date" type="date" name="due_date" value="{{ old('due_date', $task?->due_date?->format('Y-m-d')) }}"></div></div>
<div class="form-actions"><button class="button accent" type="submit">{{ $submitLabel }}</button><a class="button light" href="{{ route('tasks.index') }}">Cancel</a></div></form>
