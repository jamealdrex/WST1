@extends('layouts.app', ['title' => $task->task_name])
@section('content')<div class="intro"><div><div class="eyebrow">Task details</div><h1>{{ $task->task_name }}</h1></div><a class="button light" href="{{ route('tasks.index') }}">Back to list</a></div><section class="panel" style="max-width:700px;"><p>{{ $task->description ?: 'No details added.' }}</p><p class="meta"><span class="status">{{ $task->status }}</span>{{ $task->due_date ? 'Due ' . $task->due_date->format('M j, Y') : 'No deadline' }}</p><a class="button accent" href="{{ route('tasks.edit', $task) }}">Edit task</a></section>@endsection
